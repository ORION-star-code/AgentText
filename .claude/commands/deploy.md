执行部署前检查清单：

1. 确认当前分支是 main 或 release 分支
2. 检查是否有未提交的更改: `git status`
3. 运行完整测试套件: `npm test`
4. 检查代码质量: `npm run lint`
5. 构建生产版本: `npm run build`
6. 确认构建成功，无错误
7. 推送到远程仓库: `git push`
8. 执行部署命令（根据项目配置）

如果任何步骤失败，停止部署并报告问题。
